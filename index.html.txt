<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday Mom | 2026</title>
    
    <!-- Fonts: Mixing High-End Serif with Clean Sans -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Manrope:wght@200;400;600&family=Great+Vibes&display=swap" rel="stylesheet">

    <style>
        /* --- CORE VARIABLES & RESET --- */
        :root {
            --bg-deep: #050509;
            --bg-glass: rgba(255, 255, 255, 0.03);
            --border-glass: rgba(255, 255, 255, 0.08);
            --text-primary: #f0f0f0;
            --text-muted: #a0a0a0;
            --accent-gold: #D4AF37;
            --accent-glow: rgba(212, 175, 55, 0.4);
            --font-display: 'Cinzel', serif;
            --font-body: 'Manrope', sans-serif;
            --font-script: 'Great Vibes', cursive;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--bg-deep);
            color: var(--text-primary);
            font-family: var(--font-body);
            overflow-x: hidden;
            width: 100vw;
            height: 100vh;
            /* Hide scrollbar for immersion */
            scrollbar-width: none; 
            -ms-overflow-style: none;
        }

        body::-webkit-scrollbar { 
            display: none; 
        }

        /* --- BACKGROUND CANVAS --- */
        #cosmos {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
        }

        /* --- UI COMPONENTS --- */
        .glass-panel {
            background: var(--bg-glass);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid var(--border-glass);
            border-radius: 24px;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.5);
        }

        .btn-modern {
            padding: 1rem 2.5rem;
            background: transparent;
            border: 1px solid var(--accent-gold);
            color: var(--accent-gold);
            font-family: var(--font-display);
            font-size: 1rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            cursor: pointer;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .btn-modern:hover {
            background: var(--accent-gold);
            color: var(--bg-deep);
            box-shadow: 0 0 20px var(--accent-glow);
        }

        /* --- SECTIONS --- */
        main {
            height: 100vh;
            overflow-y: scroll;
            scroll-behavior: smooth;
            perspective: 1000px;
        }

        section {
            min-height: 100vh;
            width: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            padding: 2rem;
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 1s ease, transform 1s ease;
        }

        section.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* --- HERO SECTION --- */
        .hero-title {
            font-family: var(--font-display);
            font-size: clamp(3rem, 8vw, 6rem);
            background: linear-gradient(to right, #fff, var(--accent-gold));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 1rem;
            text-align: center;
            line-height: 1.1;
        }

        .hero-subtitle {
            font-size: clamp(1rem, 2vw, 1.5rem);
            color: var(--text-muted);
            letter-spacing: 4px;
            text-transform: uppercase;
            margin-bottom: 3rem;
        }

        .scroll-indicator {
            position: absolute;
            bottom: 40px;
            animation: bounce 2s infinite;
        }

        /* --- MEMORY GRID (Bento Style) --- */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            width: 100%;
            max-width: 1200px;
            padding: 20px;
        }

        .grid-item {
            position: relative;
            aspect-ratio: 1; /* Square */
            overflow: hidden;
            border-radius: 16px;
            cursor: pointer;
            transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .grid-item:hover {
            transform: scale(1.05) rotate(1deg);
            z-index: 2;
            box-shadow: 0 10px 40px rgba(0,0,0,0.5);
        }

        .grid-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: opacity 0.5s ease;
        }

        .grid-caption {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            padding: 20px;
            background: linear-gradient(to top, black, transparent);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .grid-item:hover .grid-caption {
            opacity: 1;
        }

        /* --- THE LETTER --- */
        .letter-container {
            max-width: 800px;
            padding: 4rem;
            text-align: left;
            position: relative;
        }

        .letter-text {
            font-size: 1.2rem;
            line-height: 1.8;
            color: var(--text-primary);
        }

        .highlight {
            color: var(--accent-gold);
            font-weight: 600;
        }

        /* --- INTERACTIVE CAKE --- */
        .cake-container {
            position: relative;
            width: 300px;
            height: 300px;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
        }

        .flame {
            position: absolute;
            top: -60px;
            width: 20px;
            height: 30px;
            background: orange;
            border-radius: 50% 50% 20% 20%;
            box-shadow: 0 0 10px orange, 0 0 20px yellow, 0 0 40px white;
            animation: flicker 0.1s infinite;
            transition: opacity 1s ease;
        }

        .flame.blown-out {
            opacity: 0;
            animation: none;
        }

        .smoke {
            position: absolute;
            top: -80px;
            opacity: 0;
            font-size: 2rem;
            color: rgba(255,255,255,0.5);
            transition: opacity 1s;
        }

        /* --- ANIMATIONS --- */
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {transform: translateY(0);}
            40% {transform: translateY(-10px);}
            60% {transform: translateY(-5px);}
        }

        @keyframes flicker {
            0% {transform: scale(1);}
            50% {transform: scale(1.1) skewX(2deg);}
            100% {transform: scale(1);}
        }

        /* --- MUSIC PLAYER --- */
        .music-ctrl {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 100;
            background: rgba(255,255,255,0.1);
            border-radius: 50%;
            width: 50px;
            height: 50px;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            border: 1px solid var(--border-glass);
        }
    </style>
</head>
<body>

    <!-- Dynamic Background -->
    <canvas id="cosmos"></canvas>

    <!-- Audio (Hidden) -->
    <audio id="bgMusic" loop>
        <source src="https://assets.mixkit.co/music/preview/mixkit-piano-reflections-22.mp3" type="audio/mp3">
        <!-- Relaxing Piano Music -->
    </audio>

    <div class="music-ctrl" onclick="toggleMusic()">
        <svg id="musicIcon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"></polygon><line x1="23" y1="9" x2="17" y2="15"></line><line x1="17" y1="9" x2="23" y2="15"></line></svg>
    </div>

    <main id="scroller">
        
        <!-- SCENE 1: HERO -->
        <section id="s1" class="visible">
            <h1 class="hero-title">Happy Birthday<br>Mom</h1>
            <p class="hero-subtitle">The heart of our universe</p>
            <button class="btn-modern" onclick="scrollToSection('s2')">Begin Journey</button>
            
            <div class="scroll-indicator">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2"><path d="M7 13l5 5 5-5M7 6l5 5 5-5"/></svg>
            </div>
        </section>

        <!-- SCENE 2: MEMORIES (Replace src with real images) -->
        <section id="s2">
            <h2 style="font-family: var(--font-display); margin-bottom: 2rem; font-size: 2.5rem;">Timeless Moments</h2>
            <div class="grid-container">
                <!-- Placeholder images from Unsplash -->
                <div class="grid-item glass-panel">
                    <img src="https://images.unsplash.com/photo-1490578474895-699cd4d2ff5f?auto=format&fit=crop&w=400&q=80" alt="Memory 1">
                    <div class="grid-caption">Elegance</div>
                </div>
                <div class="grid-item glass-panel">
                    <img src="https://images.unsplash.com/photo-1549417229-aa67d3263c09?auto=format&fit=crop&w=400&q=80" alt="Memory 2">
                    <div class="grid-caption">Strength</div>
                </div>
                <div class="grid-item glass-panel">
                    <img src="https://images.unsplash.com/photo-1513151233558-d860c5398176?auto=format&fit=crop&w=400&q=80" alt="Memory 3">
                    <div class="grid-caption">Love</div>
                </div>
            </div>
        </section>

        <!-- SCENE 3: THE MESSAGE -->
        <section id="s3">
            <div class="glass-panel letter-container">
                <h2 style="font-family: var(--font-script); color: var(--accent-gold); font-size: 3rem; margin-bottom: 1rem;">Dearest Mom,</h2>
                <p class="letter-text">
                    In a world that changes so fast, you remain my constant north star. <br><br>
                    Today isn't just about celebrating another year; it's about celebrating the <span class="highlight">grace</span>, the <span class="highlight">wisdom</span>, and the unconditional love you bring into the world every single day.
                    <br><br>
                    Thank you for being my teacher, my protector, and my best friend. May this year bring you as much joy as you give to everyone around you.
                    <br><br>
                    With all my heart, <br>
                    <span style="font-family: var(--font-script); font-size: 2rem;">Your Child</span>
                </p>
            </div>
        </section>

        <!-- SCENE 4: INTERACTIVE CAKE -->
        <section id="s4">
            <h2 style="font-family: var(--font-display); margin-bottom: 2rem;">Make a Wish</h2>
            <p style="color: var(--text-muted); margin-bottom: 2rem;">Click the flame to blow out the candle</p>
            
            <div class="cake-container" onclick="blowCandle()">
                <!-- SVG Cake Illustration -->
                <svg width="200" height="200" viewBox="0 0 200 200">
                    <!-- Base -->
                    <rect x="50" y="100" width="100" height="60" rx="5" fill="#554466" stroke="#ffffff" stroke-width="2"/>
                    <rect x="45" y="100" width="110" height="10" rx="2" fill="#D4AF37"/>
                    <!-- Top -->
                    <ellipse cx="100" cy="100" rx="50" ry="15" fill="#665577" stroke="#ffffff" stroke-width="1"/>
                    <!-- Candle -->
                    <rect x="95" y="60" width="10" height="40" fill="#eee"/>
                    <line x1="100" y1="60" x2="100" y2="55" stroke="black" stroke-width="2"/>
                </svg>
                <!-- Interactive Elements -->
                <div class="flame" id="flame"></div>
                <div class="smoke" id="smoke">💨</div>
            </div>
            
            <h2 id="bday-msg" style="margin-top: 2rem; opacity: 0; transition: opacity 2s; color: var(--accent-gold); font-family: var(--font-script); font-size: 4rem;">Happy Birthday!</h2>
        </section>

    </main>

    <script>
        // --- 1. BACKGROUND PARTICLE SYSTEM (CANVAS) ---
        const canvas = document.getElementById('cosmos');
        const ctx = canvas.getContext('2d');
        let width, height;
        let particles = [];

        function resize() {
            width = canvas.width = window.innerWidth;
            height = canvas.height = window.innerHeight;
        }
        window.addEventListener('resize', resize);
        resize();

        class Particle {
            constructor() {
                this.x = Math.random() * width;
                this.y = Math.random() * height;
                this.size = Math.random() * 2;
                this.speedX = (Math.random() - 0.5) * 0.5;
                this.speedY = (Math.random() - 0.5) * 0.5;
                this.opacity = Math.random() * 0.5;
            }
            update() {
                this.x += this.speedX;
                this.y += this.speedY;
                if (this.x < 0) this.x = width;
                if (this.x > width) this.x = 0;
                if (this.y < 0) this.y = height;
                if (this.y > height) this.y = 0;
            }
            draw() {
                ctx.fillStyle = `rgba(255, 255, 255, ${this.opacity})`;
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
            }
        }

        // Initialize particles
        for (let i = 0; i < 150; i++) particles.push(new Particle());

        function animate() {
            ctx.clearRect(0, 0, width, height);
            
            // Add subtle gradient background logic per frame for "breathing" effect
            const time = Date.now() * 0.001;
            const r = Math.floor(10 + Math.sin(time) * 5);
            document.body.style.backgroundColor = `rgb(${r}, 5, 15)`;

            particles.forEach(p => {
                p.update();
                p.draw();
            });
            requestAnimationFrame(animate);
        }
        animate();

        // --- 2. SCROLL OBSERVER (REVEAL ANIMATIONS) ---
        const observerOptions = {
            threshold: 0.3
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('section').forEach(sec => {
            observer.observe(sec);
        });

        function scrollToSection(id) {
            document.getElementById(id).scrollIntoView({ behavior: 'smooth' });
        }

        // --- 3. AUDIO LOGIC ---
        const audio = document.getElementById('bgMusic');
        const icon = document.getElementById('musicIcon');
        let isPlaying = false;

        function toggleMusic() {
            if (isPlaying) {
                audio.pause();
                icon.innerHTML = `<polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"></polygon><line x1="23" y1="9" x2="17" y2="15"></line><line x1="17" y1="9" x2="23" y2="15"></line>`;
            } else {
                audio.play();
                icon.innerHTML = `<polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"></polygon><path d="M19.07 4.93a10 10 0 0 1 0 14.14M15.54 8.46a5 5 0 0 1 0 7.07"></path>`;
            }
            isPlaying = !isPlaying;
        }

        // --- 4. INTERACTIVE CAKE LOGIC ---
        let candleBlown = false;

        function blowCandle() {
            if (candleBlown) return;
            
            const flame = document.getElementById('flame');
            const smoke = document.getElementById('smoke');
            const msg = document.getElementById('bday-msg');

            flame.classList.add('blown-out');
            
            // Trigger smoke
            smoke.style.opacity = 1;
            setTimeout(() => { smoke.style.opacity = 0; }, 2000);

            // Trigger confetti
            launchConfetti();

            // Reveal Message
            setTimeout(() => {
                msg.style.opacity = 1;
            }, 500);

            candleBlown = true;
        }

        // --- 5. CONFETTI (Simple Canvas Implementation) ---
        function launchConfetti() {
            // A quick burst of confetti
            for(let i=0; i<100; i++) {
                createConfettiPiece();
            }
        }

        function createConfettiPiece() {
            const colors = ['#D4AF37', '#ffffff', '#ff69b4', '#87CEEB'];
            const conf = document.createElement('div');
            conf.style.position = 'fixed';
            conf.style.width = Math.random() * 10 + 5 + 'px';
            conf.style.height = Math.random() * 5 + 5 + 'px';
            conf.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
            conf.style.left = Math.random() * 100 + 'vw';
            conf.style.top = '-10px';
            conf.style.zIndex = '9999';
            conf.style.transition = 'top 3s ease-out, transform 3s ease-out';
            document.body.appendChild(conf);

            setTimeout(() => {
                conf.style.top = '110vh';
                conf.style.transform = `rotate(${Math.random() * 360}deg)`;
            }, 10);

            setTimeout(() => {
                conf.remove();
            }, 3000);
        }
    </script>
</body>
</html>